{
  "mcpServers": {
    "context-keeper": {
      "command": "npx",
      "args": ["context-keeper-mcp", "--transport", "stdio"],
      "env": {
        "STORAGE_BACKEND": "memory",
        "DB_FILE_PATH": "context.sql"
      }
    }
  }
}
